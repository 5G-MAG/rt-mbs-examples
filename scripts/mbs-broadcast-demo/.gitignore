# Everything this demo writes while running: generated configs, logs, PID files, the MBSF
# response cache and the IDs of the service/session a run created. All of it is per-run state,
# regenerated by the scripts, and it reaches tens of megabytes within an hour of running.
run/
